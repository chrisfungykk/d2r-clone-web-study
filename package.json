{
  "name": "d2r-clone-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "vitest run",
    "lint": "biome check . && node scripts/check-sim-purity.mjs",
    "ip-audit": "node scripts/ip-audit.mjs",
    "size": "node scripts/check-size.mjs",
    "perf": "node scripts/run-perf.mjs",
    "replay": "node headless/replay.ts",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.185.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/node": "^24.0.0",
    "@types/three": "^0.185.0",
    "playwright": "^1.61.0",
    "typescript": "^6.0.0",
    "vite": "^8.0.0",
    "vitest": "^4.0.0"
  }
}
